## 1. Implementation
- [ ] 1.1 Create `preprocessing.py` to read raw CSV and produce cleaned CSV
- [ ] 1.2 Create `train.py` to train LogisticRegression with TF-IDF, save `models/model.joblib` and `models/vectorizer.joblib`
- [ ] 1.3 Create `predict.py` CLI to predict single messages or batch CSV
- [ ] 1.4 Add unit tests for preprocessing and training (optional in HW)

## 2. Notebook & Visuals
- [ ] 2.1 Create `notebooks/spam_classifier.ipynb` with Traditional Chinese markdown explanations before each code cell
- [ ] 2.2 Include visualizations: class distribution, token replacements, top tokens by class, ROC, Precision-Recall, threshold sweep

## 3. Deployment
- [ ] 3.1 Create `app.py` (Streamlit) with controls and live inference
- [ ] 3.2 Document running instructions and dependency manifest (`requirements.txt`)

## 4. Report
- [ ] 4.1 Write `report.md` describing process and how OpenSpec guided the work
